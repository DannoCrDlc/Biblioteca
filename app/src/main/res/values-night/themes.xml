<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Duelistas" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_night</item>
        <item name="colorPrimaryVariant">@color/main_night</item>
        <item name="colorOnPrimary">@color/main_night</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/main_night</item>
        <item name="colorSecondaryVariant">@color/secundary_night</item>
        <item name="colorOnSecondary">@color/base_night</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="SplashTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_view</item>
    </style>
</resources>